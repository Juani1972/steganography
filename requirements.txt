# Core dependencies (always required)
numpy>=1.24.0
pillow>=10.0.0
click>=8.1.0
rich>=13.0.0
cryptography>=42.0.0
argon2-cffi>=23.1.0
reedsolo>=1.7.0

# Full steganography (install with: pip install -e ".[full]")
scipy>=1.11.0
opencv-python>=4.8.0
scikit-image>=0.22.0

# Nostr support (install with: pip install -e ".[nostr]")
websockets>=12.0
aiohttp>=3.9.0
secp256k1>=0.14.0

# Real-world social media adapters (install with: pip install -e ".[social]")
# NOTE: these were used by stegstr/platform/adapters/*.py but were previously
# missing from this file entirely.
requests>=2.31.0
tweepy>=4.14.0
praw>=7.7.0
ngrok>=1.0.0
flask>=3.0.0   # GUI local (stegstr/gui/web_app.py)

# Development (install with: pip install -e ".[dev]")
pytest>=7.4.0
pytest-cov>=4.1.0
hypothesis>=6.88.0
flake8>=6.0.0
pylint>=2.17.0
black>=23.0.0
isort>=5.12.0
bandit>=1.7.0
safety>=2.3.0

# Optional utilities
pynacl>=1.5.0
bech32>=1.2.0
qrcode>=7.4.0
